/** @format */

export interface MinMileageCarsDto {
    minYear: number;
    maxYear: number;
    minEngine: number;
    maxEngine: number;
    brandIds: number[];
    modelIds: number[];
    editionIds: number[];
}
